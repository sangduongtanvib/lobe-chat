'use client';

import { PropsWithChildren } from 'react';

/**
 * Combined sidebar component for handling both topic and session sidebars
 */
const CombinedSidebar = ({ children }: PropsWithChildren) => {
  return children;
};

export default CombinedSidebar;