'use client';

import { PropsWithChildren } from 'react';

/**
 * Server-side topic provider component
 */
const ServerTopicProvider = ({ children }: PropsWithChildren) => {
  return children;
};

export default ServerTopicProvider;