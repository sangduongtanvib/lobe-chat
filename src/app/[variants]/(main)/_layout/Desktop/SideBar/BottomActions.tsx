import { memo } from 'react';
import { Flexbox } from 'react-layout-kit';

const BottomActions = memo(() => {
  return (
    <Flexbox gap={8}>
      {/* GitHub and Documentation links have been removed */}
    </Flexbox>
  );
});

export default BottomActions;
