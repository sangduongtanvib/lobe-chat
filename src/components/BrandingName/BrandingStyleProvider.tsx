'use client';

import '@/styles/branding.css';

/**
 * A component that ensures the branding styles are loaded
 * This component should be included in the app layout to ensure
 * the branding styles are loaded globally
 */
const BrandingStyleProvider = () => {
  return null;
};

export default BrandingStyleProvider;
